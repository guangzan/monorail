{
  "name": "monorail",
  "version": "0.1.0",
  "private": true,
  "description": "Local-first agent skills for align → spec → slice → build",
  "repository": {
    "type": "git",
    "url": "https://github.com/guangzan/monorail"
  },
  "scripts": {
    "changeset": "changeset",
    "version": "changeset version"
  },
  "devDependencies": {
    "@changesets/changelog-github": "^0.7.0",
    "@changesets/cli": "^2.30.0"
  },
  "overrides": {
    "@types/node": "^22.15.0"
  }
}
