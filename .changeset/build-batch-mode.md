---
"monorail": minor
---

`/rail-build` gains **Batch mode**: several small frontier issues run sequentially in one session — fresh implementer sub-agent per issue, seams confirmed once up front, no check-ins between issues. Sequential throughput; parallel builds still require one worktree per issue.
